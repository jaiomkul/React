import { useEffect } from "react"

export const EmployeeList = () => {

    useEffect(
        async function getData() {
            const data = await fetch("")
        }
    )

    return (

    )
}